# Decisions

技術判断の経緯ログ。`dwg7/height-coverage`の`DECISIONS.md`と同じ位置づけ（結論だけでなく「なぜ」を残す）。

## 1. Virgoデータの実測検証（2026-09-02）

CLAUDE.md記載のスキーマ・配色表を鵜呑みにせず、着手前に実際のエンドポイントを叩いて検証した。

- WFS (`outputFormat=application/json`): 63フィーチャー、EPSG:32648、属性キー（`zoning`/`zone_name`/`h`/`e`/`cos`）とも記載通り
- GetLegendGraphic (`format=application/json`): 19ゾーンコード全ての`fill`/`stroke`をJSONで直接取得。CLAUDE.mdの配色表と完全一致

**内部データストアがShapefileかどうかは問題にならなかった** — WFSが`outputFormat=application/json`を素直に受け付け、サーバー側でGeoJSONを返すため、追加の形式変換は不要だった。

## 2. PMTilesのホスティング：ローカルパス方式（GitHub rawではなく）

当初「小さいファイルだからGitHub raw URL経由のリモートソースでいいのでは」と考えたが、stars運用エージェント（stars-fd）が実測で否定した：**raw.githubusercontent.comは`accept-ranges: bytes`ヘッダーを返すが、実際のRangeリクエストには206ではなく常に200（全文）で応答する**。PMTilesはRangeリクエストによる部分読み出しが前提のフォーマットなので、技術的に正しい土台ではない。

正しい方式は、`hfu/stars`の`data/.gitignore`が`*.pmtiles`を除外している通り、**実ファイルはGit外（rsync/scp）で転送し、`config/martin.yaml`にはローカルパスで登録する**（`pmtiles_jma_1saibun_hkd`等の既存パターンと同じ）。同一マシン上のセッション間だったため、ファイルパスを伝えるだけで転送が完了した（[hfu/stars#7](https://github.com/hfu/stars/pull/7)）。

## 3. 「地図が真っ黒になる」バグ：MapLibreのコンテナサイズ誤測定

実装直後、ブラウザでテストしたところ地図が完全に黒く表示され、コンソールにもエラーが出ない状態に遭遇した。

調査の結果：
- スタイル自体は正常にロードされていた（`map.isStyleLoaded()`はいずれtrueになる、レイヤー構成も正しい）
- 原因は`<canvas>`のCSSサイズが実際のコンテナ（1280×720）ではなく400×300に固定されていたこと
- さらに調査すると、**このブラウザ自動化ツールの「ペインが非表示（hidden）」の間、コンテナのgetBoundingClientRectが0×0を返す**ことが判明。MapLibreは`new Map()`のコンストラクタ実行時にコンテナサイズを一度だけ測定してcanvasに焼き込み、以後は自動で再測定しない。ペインが非表示の間に初期化されると、その瞬間の（誤った）サイズがそのまま固定される

**教訓**：これは実際のユーザー環境で普通に起きうる話でもある（タブがバックグラウンドで読み込まれた場合など）。修正として、`window`の`resize`イベントと`document`の`visibilitychange`イベント（`visible`になった瞬間）の両方で`map.resize()`を呼ぶようにした。これによりコンテナが後から正しいサイズを報告するようになった時点で自動的に復旧する。

副産物として、**このブラウザツールでの検証中に「ペインが非表示」と表示された直後は測定値を疑うこと**をこのプロジェクトの検証Tipsとして記録しておく（height-coverage-a5も同様の「自動化ツールが劣化してMapLibre公式デモすら読めなくなることがある」という警告をくれていたが、今回の実際の原因はツールの劣化ではなく、非表示中のビューポート測定値の問題だった）。

## 4. UIデザイン：独自性より先行事例の踏襲を優先（2026-09-02、ユーザー指示）

ユーザーからの明示的な方針：「UIについては『独自性』よりは『パターンをよく踏まえている』ことを優先。適切に謝辞を入れながら、なるべく先行者の知恵を取り込んでください」。

以下の改修は、この方針のもとで行った：

- **ゾーンコードの地図上ラベル表示**：NYC Planning Labs「ZoLa」（zola.planning.nyc.gov、後述）が、ズームすると各ゾーニング区画に太字でゾーンコードを直接ラベル表示するパターンを踏襲。`text-font: "Noto Sans Bold"`、`text-size: 14`、`minzoom: 10`に変更（元は`Noto Sans Regular`・11px・minzoom 11）
- **ホバーパネルの再設計**：ゾーン名を大きく太字で見出しに（初見の訪問者が「これは何のサイトか」を一目で理解できるように）、高さ制限・建蔽率・容積率をビビッドな色のスタットチップとして表示。技術的な生の属性値は小さいモノスペースで下部に残す（既存の「小さい文字で技術詳細が出るのはいい」というユーザーの評価を維持しつつ、視覚的な主役をゾーン名・規制値に譲る）
- **建物線画の色調管理**：当初OSM由来/非OSM由来の建物輪郭線を明度差（濃いグレー/薄いグレー）で塗り分けていたが、ユーザーから「ゾーニングの塗りの上に乗っているため、明度差が本来意図しない『別の意味』を持つように見える」という指摘。色彩のトーンマネジメント理論に従い、**明度・色相を統一し、ダッシュパターン（実線/破線）と線幅で切り替える**方式に変更
- **背景線画は塗りを避ける（できるだけ、少なくとも濃い塗りは避ける）という原則の再確認**：`park`/`water`/`landcover_ice_shelf`/`landcover_glacier`/`landuse_residential`/`landcover_wood`/`aeroway-area`/`road_area_pier`はすべて`fill`から`line`へ変換済みで、最終的な合成スタイルに残る`fill`タイプのレイヤーは`zoning-fill`（意図的な例外）と`background`（背景色、rgb(242,243,240)という非常に淡い色のみ）だけであることをブラウザ上で実際にfetchして検証済み（2026-09-02）。新しい背景系レイヤーを追加する際は、この不変条件（fillはゾーニング層専有、背景色は淡い色のみ）を破らないこと

### 建物線と道路の色調の食い違い（追加の気づき）

上記の建物線変更の第一版では、OSM/非OSM問わず同じ濃いグレー（`rgb(110,110,110)`）を採用したが、ユーザーから「道路の強い白と、建築物外周線の暗い色がちょっとマッチしていない」という指摘。他の変換済み背景線画（水域・土地利用等）はPositron本来の淡い色（`fill-color`をそのまま流用、実測でrgb(190〜234)程度の明るいグレー）を引き継いでいるのに対し、建物線だけ独自に大きく異なる濃さを選んでしまったことが原因。**背景線画全体で単一の淡いトーン（`LINE_COLOR = rgb(180,182,178)`）に統一**し、道路（白いケーシング）が視覚的な主役、建物・土地利用線は静かな脇役、という本来のPositronの階層を回復した。

## 5. NYC Planning Labs（ZoLa）の調査（2026-09-02）

ユーザーの依頼で、さっぽろのサービスに加えてNYC Planning Labsの現状も調査した。

**沿革**：2017年6月、DCPのシビックテクノロジスト Chris Whong により設立。Danny Fuchs（Capital Planning部門長）・Hassan Adekoya（CIO）・Jon Kaufman（COO）の後押しを受けた。「オープン技術・アジャイル開発・ユーザー中心設計で、プランナーと一緒にインパクトのあるプロダクトを作る」ことを掲げ、"build with, not for" the users を原則とした小回りの利く内部チームとしてスタート（[出典](https://medium.com/nyc-planning-digital/ux-on-the-30th-floor-8-months-with-planning-labs-and-nycs-department-of-city-planning-69f3b6a2c9cc)）。

**この1年〜3ヶ月の状況（2026-09調査時点）**：
- `labs.planning.nyc.gov`・`planninglabs.nyc`という「About/ブランドサイト」はTLS証明書が失効しており、事実上更新が止まっている
- 一方、**プロダクト本体（ZoLa: `NYCPlanning/labs-zola`リポジトリ）は非常にアクティブ**：直近コミットは2026-08-20（調査時点から2週間以内）、issueは138件オープン、star 96。直近数ヶ月のコミット内容も「本番用APIエンドポイントへの切り替え」「デプロイ手順のREADME追記」など、実運用に根ざした地に足の着いた作業が続いている
- `NYCPlanning` GitHub Organization全体を見ると、`data-engineering`・`gis-nycplanning`・`equity-tool`・`ae-zoning-api`など多数のリポジトリが調査時点の前日・当日にpushされており、組織全体としての技術活動は非常に活発
- 2017年発足時の「Planning Labs」という単一チームから、その後「Data Engineering」チーム（Enterprise Data Management部門傘下）等が並存する形に組織が拡張したとみられる（一方が他方に置き換わったわけではない）

**運用上の教訓（このプロジェクト・dwg7全体の運用に活かす）**：
1. **「ブランド／About」ページと「プロダクト」の鮮度は別物**。ラベルや看板（About ページ）が古びても、実働するツール（ZoLaのリポジトリ・issue対応）が生きていれば実質的な価値は失われない。README/CLAUDE.mdのような「実務ドキュメント」は割ける労力を優先的にそちらに回すべきで、装飾的な「About us」的記述に時間をかけすぎない
2. **小さく機動的なチーム（Labs）と、より大きな組織内チーム（Data Engineering）が並存・拡張していく形**は、dwg7が単一の万能セッションではなく、height-coverage・stars・cafebabe（知見集約）・このセッションのように役割特化した複数エージェントが協調する現在のスタイルの妥当性を裏付ける
3. **公開GitHub issueを一次のフィードバック窓口にする**運用（ZoLaのAboutページに明記された"Application Feedback: add a GitHub Issue"）は、このプロジェクトが`unopengis/7`にissueを立てて進捗を公開報告するのと同じ発想。透明性のある開発ログが、後から参照可能な資産になる
4. Planning Labsが標榜した"build with, not for" the users・実際のソフトウェアを実際のユーザーでテストする、という姿勢は、今回このプロジェクトでユーザーに都度スクリーンショットを見せながらリアルタイムでフィードバックを受けて反復した進め方と一致する。机上のスペックを積み上げるより、動くものを早く見せる方が良いという裏付けになった

**謝辞**：ZoLaはUI（ゾーンコードの地図上ラベル表示等）の参照元として、README.mdおよびdocs/index.htmlの謝辞に明記済み。

## 6. カートグラフィーの原則：ラベルは常にすべての塗り・線画の上（2026-09-02）

ユーザーからの実地の指摘：「Ta や UEb がベースマップで踏まれているのが惜しい。注記はすべての塗りや線画の上にくる。」

原因はPositronスタイル自身のレイヤー順序の癖にあった。`water_name`という`type: "symbol"`のレイヤーが、`landcover_wood`の直後という非常に早い位置（道路・鉄道網や建物レイヤーより前）に存在する。当初の実装は「最初のsymbolレイヤーの直前」に`zoning-label`を含む新規レイヤー群を挿入していたため、`zoning-label`が`water_name`のすぐ手前＝道路・鉄道網や建物線のレイヤーより**前**（＝下）に来てしまい、後から描画される道路・建物の線に踏まれる結果になっていた。

**修正**：`zoning-fill`/`zoning-outline`/建物線はそのままの挿入位置を維持しつつ（半透明の塗りの上に道路の白いケーシングが乗るという、既に確認済みの見た目を壊さないため）、`zoning-label`だけを分離し、**スタイル合成の最後に「symbolタイプのレイヤーを全て配列の末尾へ安定ソートする」パスを追加**した。これにより、Positron自身が元から持つ`water_name`等のラベルも含め、あらゆるラベルが塗り・線画より必ず上に来るようになる。

**一般化した原則**：このプロジェクトに限らず、複数のスタイルを合成する（ベーススタイルをfetchしてレイヤーを足し引きする）実装をする際は、「最初のsymbolレイヤーの位置」を機械的に信頼せず、**最終的な合成後に改めてlabel-vs-fill/lineの順序を明示的に保証するパスを入れる**べき。ベーススタイルのレイヤー順序は往々にして「意味的にlabelが最後にまとまっている」という前提が崩れていることがある（今回のPositronの`water_name`のように）。

## 7. ゾーニング塗りの不透明度：ZoLaの実装コードを参照して固定値をやめる（2026-09-02）

ユーザーからの指摘：「ゾーンの塗り色が濃すぎて地図が見えにくい。さっぽろでは透過っぽくしているように思える。世界の好例を横断的に見て改善できないか。」

当初`fill-opacity: 0.55`をズームに関わらず固定値で使っていた。CLAUDE.mdの当初の指示（「不透明度を50〜60%程度に落として」）に沿った値ではあったが、実際にVirgoの原色（例：UAaの`#FF0101`のような純度の高い色）に適用すると、実機で見て確かに重く、下の基本図の線画が埋もれがちだった。

**調査**：さっぽろのサイトは実機での再確認が（レガシーな地図エンジンのため）うまくいかなかったが、NYC Planning Labsの「ZoLa」は**オープンソース**（`NYCPlanning/labs-zola`）なので、印象論ではなくコードそのものを確認できた。実際の`public/layer-groups.json`内、ゾーニング地区の塗りレイヤー`zd-fill`の`fill-opacity`は：

```
"fill-opacity": { "stops": [[15, 0.3], [16, 0]] }
```

つまり**z15で0.3、z16以降は完全に透明**。線（`zd-lines`）側は逆に`line-width`が1→3、`line-opacity`が0→0.5とズームインするほど強くなる。**塗りは「今どのエリアか」を大づかみに伝える役割に限定し、パーセル（建物・街区）スケールに近づくほど、線とラベルに主役を譲る**という設計だと分かった。

**反映**：Virgoの色相そのものは変えない（CLAUDE.mdの明示的な指示：Virgoの実際の配色をそのまま使う）。**不透明度だけをズーム依存の`interpolate`式に変更**：z10で0.4、z14で0.32、z18で0.2（ZoLaのような完全透明までは落とさない——GLUP2030のゾーンはNYCの細かい街区ごとのdistrictと違い、建物スケールでもまだ意味を持つ大きさのため）。`zoning-outline`の`line-width`もz18で2まで伸ばし、塗りが薄くなる分を線が補うようにした。

**教訓**：印象だけで「なんとなく濃い/薄い」を議論するより、**オープンソースの先行事例は実際のスタイル定義（JSON）を直接読める**という強みがある。今後スタイルの微調整で迷ったら、まず該当プロジェクトがOSSかどうかを確認し、OSSならコードを読みに行くこと（この教訓は、cafebabeへ共有した「データ出自調査」パターンとも通じる——外部の先行事例も、可能な限り一次情報＝コードそのものに当たるべき）。

## 8. `h`/`e`/`cos`が`0`の場合は「未設定」であり「規制値ゼロ」ではない（2026-09-05）

ユーザーからの指摘：「Taで属性が0,0,0となっており、表示が0m,0%,0%となっているが、これは-,-,-と表示するべきだろう。N/A的な意味なんだと思う」。

実データを確認すると、Ta（空港交通区）・Em（軍事区）・NE（公共利用保全区）・T（交通区）・UEb（市街開発促進区）は`h=0 e=0 cos=0`。一方N（農業区）は`h=10`だが`e=0 cos=0`——**3項目が常にセットでゼロになるわけではなく、各フィールドが独立に「未設定」でありうる**。トランスポート・軍事区画に「高さ制限0m・建蔽率0%・容積率0%」という規制がそのままの意味で存在するとは考えにくく、Virgoのデータモデルでは単に値が入力されていないことを`0`で表現していると判断した。

**修正**：`h`/`e`/`cos`それぞれについて、値が`0`のときは`-`を表示するヘルパー関数（`formatStat`）を導入。3フィールドを一括判定せず、フィールドごとに独立にチェックする（Nのケースに対応するため）。ホバーパネル下部の生データ行（`zoning=Ta h=0 e=0 cos=0`）はそのまま残し、技術的な参照は失わない。

**留意点**：この判断はユーザーの解釈に基づくものであり、Virgoの正式な規定文書を確認して確定させたわけではない（GLUP2030の原文には当たっていない）。「N（農業区）のe=0が実際に『建築禁止』という意味のある規制なのか、単なる未入力なのか」は、今回はユーザーの判断（N/A扱い）を採用したが、将来的に疑義が生じたら再検証すること。

## 9. ゾーニングポップアップにGoogle Street Viewリンクを追加（height-coverageから移植、2026-09-05）

`dwg7/height-coverage`が同種の機能（建物ポップアップへのStreet Viewリンク）を先行実装（[e76ea1c](https://github.com/dwg7/height-coverage/commit/e76ea1c)、判断経緯は同リポジトリのDECISIONS.md項目12）。height-coverage-a5から実装内容の共有を受け、`showZoningPopup()`（`docs/app.js`）にそのまま移植した。

**URLスキーム**：`https://www.google.com/maps/@?api=1&map_action=pano&viewpoint={lat},{lng}&fov=90` — APIキー・署名・従量課金いずれも不要なプレーンURL。Street View Static API / JS Embed APIは使わない（どちらもAPIキー＋署名＋課金が必須）。

**座標**：クリックイベントの`e.lngLat`をそのまま使用（ポリゴン重心の計算はしない）。`heading`/`pitch`は指定しない——省略するとGoogleが最寄りの撮影地点から自動的に妥当な方向を向いてくれるため、方位計算が不要になる。Street View撮影実績が無い場所でも、Googleが通常の地図表示にフォールバックするだけなので、事前のカバレッジ判定も不要。

**UI上の区別**：ゾーン情報（高さ制限・建蔽率・容積率）とは`border-top`区切り線で視覚的に分離し、下線付きのプレーンテキストリンクとして表示（ボタンではない）。直下に「Just for a look -- not a source to trace over for editing.」の注記を必ず添える。height-coverageと同じ設計判断——「見るためのリンク」と「編集を後押しするアクション」（このプロジェクトには編集リンク自体がないが、規制値という一次情報）を混同させないため。`target="_blank" rel="noopener"`必須。（この節の内容はいずれも項目10・11で更新されている——`target`は固定名に変更、UIは下線リンクからコンパスへ変更）

## 10. Street ViewリンクのtargetをタブReuse可能な固定名に変更（2026-09-05）

ユーザー指摘：「ページ遷移、aタグにtarget="_vientianne_streetview"というようにつける感じで。我々が消えてしまう必要はない」。`target="_blank"`のままだとクリックのたびに新しいタブが増え続ける。`target`を固定文字列（`_vientiane_streetview`、プロジェクト名の綴りに合わせた）にすることで、2回目以降のクリックは同じタブ/ウィンドウに遷移し、地図タブ自体は残り続ける。

**留意点**：Claude Browser（このプロジェクトの検証に使っている自動化ブラウザツール）は名前付きtargetと`_blank`の挙動を区別せず同一タブ内で遷移してしまうため、複数タブでの正しい再利用挙動は実ブラウザ（Chrome/Safari等）でのみ確認可能。コード自体は標準のnamed target挙動に従っているだけなので実ブラウザでは問題ないはずだが、記録として残す。

## 11. Street Viewリンクを8方向コンパスに変更、重複するゾーン情報をポップアップから削除（2026-09-05）

**背景**：ユーザー指摘（2点）。(1)「クリックポップアップの『Zpp-Ua — Historic-Town Conservation Zone / Height limit.../Coverage.../Floor area...』は左下（ホバーパネル）と重複があるから要らない」。(2)「方角が代替指定できるといいな。クリックするとマウスカーソルの周り八方向にボタンが表示されて、それぞれの方向について検索がかかるように。関心の方向と『大体同じ向き』であることが期待されていて、『逆方向』だと『酔う』感じになる」。

**対応(1)**：`showZoningPopup()`からゾーンコード・ゾーン名・h/e/cosの表示ブロックを削除。ホバーパネル（`showHoverInfo`、左下固定表示）に同じ情報が常時出ているため、クリックポップアップの役割はStreet View起動に一本化した。

**対応(2)**：クリックポップアップの中身を、単一の「View on Google Street View」リンクから、3×3グリッドの8方向ボタン＋中央「auto」ボタンに変更（`COMPASS_DIRECTIONS`定数）。各ボタンは`heading`パラメータ付きのURLを持ち、選んだ方角に近い向きでパノラマが開く。中央ボタンのみ`heading`を省略し、既存の「Googleに任せる」挙動（最寄りの撮影地点から妥当な方向を自動選択）を維持。

**方位計算の注意**：MapLibreの`bearing`は「画面の『上』が指す実際の方角」を表す（初期値0＝北が上）。地図を回転させた状態でこのプロジェクトの`NavigationControl`を使うと、画面上の「上向き矢印」は真北を指さなくなる。ボタンの矢印はあくまで**画面上の見た目の向き**（screenOffset: 上=0, 右=90, ...）で配置しているため、実際にGoogleへ渡す`heading`は`(screenOffset + map.getBearing()) % 360`で真の方位に変換している。これを怠ると、地図を回転させた状態でユーザーが「右上」を押したのに実際には全く違う方角を見せられる、というまさにユーザーが懸念した「酔う」状況になる。

**やらなかったこと**：ボタンのラベルとしてコンパス上の方位記号（N/NE/E...）ではなく矢印記号（↑↗→...）を採用した。地図の向きに追従して視覚的に「その方向を指す」ことが直感的に伝わるのは矢印であり、方位記号は地図が回転した際にかえって誤解を招くと判断したため。

## 12. 建築物外周線を主要道路と同じ白へ、Street Viewを吹き出しからカーソル直付けのコンパスへ（2026-09-05）

**背景**：ユーザーから2点。(1)「サイトの目標がやや変更されてきているから、建築物の外周線の色を変更する。かなり明るい白、主要道路の塗り色と近い白にしてくれるかな」。(2)「クリックしたらの挙動、『吹き出しが開いてその中にボタン』なのではなく、『マウスポイントの周囲に直接ボタン』が置かれるように」。

**対応(1)**：`BUILDING_LINE_COLOR`を`LINE_COLOR`（`rgb(180,182,178)`、水域・土地利用等と同じ淡いグレー）から`#ffffff`へ変更。これはPositronの主要道路の実際の色（`highway_major_inner`/`highway_motorway_inner`が共に`#fff`）と完全に一致させたもの。項目4で「建物がroadより視覚的にうるさくならないように」と明示的に決めた判断の反転にあたるが、ユーザーの言う「サイトの目標の変化」——このプロジェクトの核心的メッセージが「基本図データ（建物形状）の充実度そのものを見せる」ことである以上、建物の外周線は道路網と同じ重みで見えてよい、むしろそうあるべき——を踏まえた意図的な変更。OSM由来/非由来の区別は引き続き明度ではなくダッシュパターン・線幅で行う（項目4の該当部分は継続）。

その後ユーザーから「もう少しだけ弱くして」との追加指摘。色相（白）はそのまま道路と揃えつつ、`line-opacity: 0.85`（`BUILDING_LINE_OPACITY`）を追加して強さだけを一段引いた——色を道路と別の色に変えるのではなく、同じ白の強度を調整するアプローチを採用したのは、「道路網と同じ重みで見えるべき」という本来の意図を保ったまま微調整するため。

**対応(2)**：`maplibregl.Popup`（吹き出し・尻尾つきのカード）を使うのをやめ、素のDOM要素（`position: fixed`の`<a>`8個）をクリック地点の周囲に直接配置する自作コンポーネントに置き換えた（`openCompassMenu()`）。`document.body`に追加し、ビューポート座標系で位置決めするため、地図コンテナの`overflow:hidden`によるクリッピングを受けない。

- 閉じるタイミング：他の場所をクリック（`document`の`click`リスナー、コンパス自身へのクリックは`.contains()`判定で除外）／Escapeキー／地図の`movestart`（パン・ズーム・回転でクリック地点のスクリーン座標が古くなるため）
- 同一クリックでの誤発火に注意：ゾーニング塗りをクリックしてコンパスを開いたその同じクリックイベントが、そのまま`document`の「外側クリックで閉じる」リスナーにまで伝播すると、開けた瞬間に閉じてしまう。`e.originalEvent.stopPropagation()`で対処（後述の一時ダイアログの「OK」クリックでも同型の問題が起きたため同様に対処）。

**中央の「おまかせ」ボタンを廃止**：当初は8方向の外周に加え、中央に「headingを指定しない（＝Googleが最寄りの撮影地点から自動選択）」ボタンを1個追加していたが、ユーザーからの指摘（「これって、中央の丸は必要？」）で削除した。理由は2つ——(a) Googleは`heading`を省略した場合と同じ挙動を単に「不要なら押さない」で実現できるため独立したボタンにする価値が薄い、(b) このボタンは構造上クリック地点と完全に同じ座標に配置されることになり、コンパスを開いたそのクリックの一部を誤って拾ってしまうリスクがあった（検証中に実際にGoogleマップへ意図せず遷移する現象を観測——自動化ブラウザツール固有の挙動の可能性が高いが、クリック地点直下にクリック可能要素を置くこと自体がハザードなので、リスクごと除去した）。

**注意書きの扱いの変更**：ユーザー提案（「注意書きは『最初のクリックの際にダイアログボックスが出る』で実装するといいかな」）を受け、リング周りに常時表示していた小さな注意テキストを廃止し、代わりに**ブラウザ初回のみ**（`localStorage`の`vientianeStreetViewNoticeShown`キーで判定）中央モーダルダイアログとして表示するように変更（`showStreetViewNotice()`）。2回目以降のクリックではダイアログを出さず、コンパスだけが直接開く——ユーザーの「カーソル周囲に直接ボタン」という要望を、初回を除いて完全に実現する形。

**注意書きへの参照リンク追加**：ユーザー提案（「Googleさんのポリシーへのハイパーリンクを入れておくと親切」「OSM Foundationなどで関係する注意書きがあればそこへのリンクも」）を受け、ダイアログ本文に2つのリンクを追加。実装前に両方とも実際にfetchして中身を確認し、ユーザーにURL・選定理由をレビューしてもらった上で反映（ユーザー承認：「それらのリンクを承認する」）。

- **Google「Google Maps/Earth 追加利用規約」**（`https://www.google.com/help/terms_maps/`）——このプロジェクトが使っているのは有料APIではなく素のURLスキーム（`maps.google.com`ディープリンク）なので、`cloud.google.com/maps-platform/terms`（有料API/SDK向け規約）ではなくコンシューマー向け規約の方を採用。「redistribute or sell any part of Google Maps or create a new product or service based on Google Maps」「use Google Maps to create or augment any other mapping-related dataset」という条文が、注意書き本文（トレースして編集の典拠にしない）の直接の裏付けになっている。
- **OpenStreetMap Wiki「Google」ページ**（`https://wiki.openstreetmap.org/wiki/Google`）——検索で最初にヒットした`wiki/No_Google`は実在しない（404）ページだったため、これを避けて実在する`wiki/Google`ページを確認の上採用。「Google Mapsの利用規約は地図データ・Street View双方についてそれを元にした別のコンテンツ作成を明示的に禁じている」という、OSM側の立場を説明する内容。

**教訓**：ユーザー未検証のまま存在しないURLを提示しそうになった（`No_Google`）。外部リンクをコード・文書に載せる前は必ずfetchして実在確認すること——これはCASE_STUDIES.mdやDECISIONS.md記載の他の外部リンクにも共通する教訓（項目7の「一次情報に当たる」姿勢の延長）。
